version: '3.8'

services:
  banking:
    image: jmbilbao25/qr-banking-app:latest
    container_name: banking-app
    ports:
      - "80:80"
    environment:
      - ECOM_CALLBACK_BASE=${ECOM_CALLBACK_BASE:-http://3.91.174.230}
      - DB_HOST=mysql
      - DB_NAME=${DB_NAME:-bankdb}
      - DB_USER=${DB_USER:-bankuser}
      - DB_PASSWORD=${DB_PASSWORD:-devpass}
      - USE_SQLITE=${USE_SQLITE:-}
    depends_on:
      - mysql
    restart: unless-stopped
    networks:
      - bank-net

  mysql:
    image: mysql:5.7
    container_name: bank-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-devroot}
      - MYSQL_DATABASE=${DB_NAME:-bankdb}
      - MYSQL_USER=${DB_USER:-bankuser}
      - MYSQL_PASSWORD=${DB_PASSWORD:-devpass}
    volumes:
      - bank_mysql_data:/var/lib/mysql
    restart: unless-stopped
    networks:
      - bank-net

volumes:
  bank_mysql_data:

networks:
  bank-net:
